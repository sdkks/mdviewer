#!/usr/bin/env bash
set -euo pipefail

MSG=$(head -1 "$1")

if echo "$MSG" | grep -qE '^(Breaking|Feature|Core|Fix|chore):'; then
    exit 0
fi

echo "Error: commit message prefix not recognised."
echo "  Got: $MSG"
echo "  Expected one of: Breaking: | Feature: | Core: | Fix: | chore:"
exit 1
